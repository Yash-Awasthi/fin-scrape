# Core
python-dotenv>=1.0.0
python-dateutil>=2.9.0
requests>=2.31.0

# Vendored Scrapling dependencies (lxml C parser, TLS fingerprinting, stealth browser)
lxml>=5.0.0
cssselect>=1.2.0
curl_cffi>=0.7.0
orjson>=3.9.0
w3lib>=2.1.0
tld>=0.13.0
browserforge>=1.1.0
msgspec>=0.18.0
typing_extensions>=4.0.0

# Browser automation (patchright = patched Playwright fork for stealth)
patchright>=1.0.0

# RSS feed parsing
feedparser>=6.0.0

# Financial data
yfinance>=0.2.30
