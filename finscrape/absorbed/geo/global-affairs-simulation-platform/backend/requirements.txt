# =====================================================
# requirements.txt - 国关推演平台依赖清单
# 更新于 2026-04 — 含所有生产级功能依赖
# =====================================================

# ── Web 框架 ─────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
slowapi==0.1.9               # API 速率限制

# ── 认证 ────────────────────────────────────────────
PyJWT==2.8.0                 # JWT 令牌生成与验证
passlib[bcrypt]==1.7.4       # 密码哈希

# ── 数据库 ───────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.2              # 数据库迁移管理
asyncpg==0.29.0              # PostgreSQL 异步驱动（可选，生产环境推荐）
greenlet==3.0.3              # SQLAlchemy async 所需

# ── 数据验证 ─────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0

# ── AI / LLM ────────────────────────────────────────
anthropic>=0.40.0            # Claude API SDK（需支持 httpx.Timeout 参数）
httpx>=0.27.0                # HTTP 客户端（anthropic 依赖，llm_router 直接使用）

# ── 新闻采集 ────────────────────────────────────────
feedparser==6.0.12           # RSS/Atom 解析（6.x 支持 handlers= 参数代理注入）

# ── PDF 导出 ─────────────────────────────────────────
reportlab==4.2.0             # PDF 生成引擎（含中文字体支持）

# ── 地理编码（可选，用于 Nominatim API）──────────────
# 若不使用 Nominatim（只用内置坐标库），可注释此行
geopy==2.4.1

# ── 监控 ────────────────────────────────────────────
prometheus-fastapi-instrumentator>=6.1.0  # Prometheus 指标

# ── 测试 ────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7

# ── 开发工具（可选）─────────────────────────────────
# black==24.4.2              # 代码格式化
# ruff==0.4.9                # 代码检查
# mypy==1.10.0               # 类型检查
