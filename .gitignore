# Environment secrets
.env

# Local-only checkpoint ledger (never push)
progress/

# Local state and data
data/*.json
data/*.json.bak
data/*.db
app.log

# Python cache
__pycache__/
*.pyc
.ipynb_checkpoints/

# uv / venv
uv.lock

# IDE settings
.vscode/
.idea/

# Virtual environment
.venv/
venv/
env/

# Distribution
*.egg-info/
dist/
build/

# Node / web build + test artifacts
node_modules/
web/test-results/
web/playwright-report/
web/.playwright/
secrets/

# Absorbed reference repos (see reference/MANIFEST.md) — not part of the project
reference/

# Absorbed heavy data (benchmarks/datasets/binaries — reference code only)
finscrape/absorbed/llm/evals/evals/registry/data/
finscrape/absorbed/quant/machine-learning-for-trading/**/*.pkl
*.jar
*.db
*.h5
*.parquet

# sqlite sidecars
data/*.db-wal
data/*.db-shm
