# Core
requests>=2.31.0
httpx[http2]>=0.27.0
python-dotenv>=1.0.0
python-dateutil>=2.9.0

# Scraping
scrapling>=0.2
feedparser>=6.0.0
beautifulsoup4>=4.12.0

# Financial data
yfinance>=0.2.30

# NLP (optional — for entity extraction)
# spacy>=3.7.0

# Playwright (required by Scrapling's StealthyFetcher/DynamicFetcher)
# Install separately: playwright install chromium
